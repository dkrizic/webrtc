name: Release Helm Charts

on:
  push:
    tags: ['[0-9]+.[0-9]+.[0-9]+']

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: azure/setup-helm@v4
      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64
          echo "a2c097180dd884a8d50c956ee16a9cec070f30a7947cf4ebf87d5f36213e9ed7  /usr/local/bin/yq" | sha256sum --check
          sudo chmod +x /usr/local/bin/yq
      - name: Set chart appVersion and image tags
        run: |
          yq e -i ".version = \"${GITHUB_REF_NAME}\"" charts/webrtc/Chart.yaml
          yq e -i ".appVersion = \"${GITHUB_REF_NAME}\"" charts/webrtc/Chart.yaml
          yq e -i ".backend.image.tag = \"${GITHUB_REF_NAME}\"" charts/webrtc/values.yaml
          yq e -i ".frontend.image.tag = \"${GITHUB_REF_NAME}\"" charts/webrtc/values.yaml
      - name: Package Helm chart
        run: |
          helm package charts/webrtc -d .helm-packages
      - name: Ensure gh-pages branch exists
        run: |
          if git ls-remote --exit-code --heads origin gh-pages; then
            echo "gh-pages branch already exists"
          else
            echo "gh-pages branch does not exist, creating it"
            CURRENT_HEAD=$(git rev-parse HEAD)
            git checkout --orphan gh-pages
            git reset --hard
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git commit --allow-empty -m "Initial gh-pages commit"
            git push origin gh-pages || { echo "Failed to push gh-pages branch"; exit 1; }
            git checkout "$CURRENT_HEAD"
          fi
      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages
      - name: Update Helm repo index
        run: |
          cp .helm-packages/*.tgz gh-pages/
          helm repo index gh-pages --url https://dkrizic.github.io/webrtc --merge gh-pages/index.yaml || \
          helm repo index gh-pages --url https://dkrizic.github.io/webrtc
      - name: Push to gh-pages
        run: |
          cd gh-pages
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Update Helm chart repo" || echo "Nothing to commit"
          git push
